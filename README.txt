NPR Project
by rlucas9

To run the project:
`python main.py [name_of_source_folder]`

Example:
`python main.py wine`

To create a new source, you must create 5 images and label them in the same way as in the source folders.